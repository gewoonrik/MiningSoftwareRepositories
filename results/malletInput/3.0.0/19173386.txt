jquery infinite scroll and   3.0 headings do not work properly  this is my infinite scroll script with jquery:      script          var start = 0;         var count = 10;          $(window).scroll(function()         {                if($(window).scrolltop() == $(document).height() - $(window).height())             {                 start += count;                  $.ajax({                     datatype:'json',                     url: 'retrieve/'+start,                     success: function(tweets)                     {                         //for(i in tweets)                         for(var i=0; i =10; i++)                         {                             $('div#tweets').append(                                 ' div class="alert alert-success" '+                                     ' div class="media" '+                                         ' div class="media-body" '+                                             ' h4 '+                                              !-- this is problem place  if i add some text scrolling function does not work--                                              ' /h4 '+                                         ' /div '+                                     ' /div '+                                 ' /div '                             );                         }                     }                 });             }         });      /script        the problem is with   h4  /h4   tag. if i include it  if($(window).scrolltop() == $(document).height() - $(window).height())  does not properly, so ajax requests. this problem increases with   h1  /h1   tag, i mean less requests are sent with ajax. i guess the problem with   3.0 headings, maybe padding and margin. how can i fix this?     edit:     for example, this snippet does not even make request to server:     $.ajax({                     datatype:'json',                     url: 'retrieve/'+start,                     success: function(tweets)                     {                         for(i in tweets)                         {                             $('div#tweets').append(                                 ' h1  /h1 '                             );                         }                     }                 });    