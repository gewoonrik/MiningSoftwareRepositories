wrapp     in a custom wordpress plugin  i try to use   in my custom wordpress plugin but seems to broke all admin area with the following     class myplugin{  public function iniadmin(){         add_action('admin_menu', array($this, 'register_collections_menu_page'));         add_action( 'admin_init', array($this,'style_ ' ));     }     public function style_ (){         wp_enqueue_script( 'jquery', '//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js', array('jquery'), '1.9.1', true); // we need the jquery library for bootsrap js to function         wp_enqueue_script( ' -js', '//netdna. cdn.com/ /3.0.0/js/ .min.js', array('jquery'), true); // all the   javascript goodness         wp_enqueue_style( ' ', '//netdna. cdn.com/ /3.0.0/css/ .min.css' );     }  }  $collections_settings = new myplugin(); if (is_admin()){     $collections_settings- iniadmin(); }      how to wrap in a propper way   style in a wordpress plugin  