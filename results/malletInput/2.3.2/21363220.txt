  and smoothscrolling javascript not playing nicely together  i have a site that uses   2 and a smooth scrolling javascript.          $('#mytab a').click(function (e) { e.preventdefault(); $(this).tab('show'); })      (   .com/2.3.2/javascript.html#tabs )    smooth scrolling javascript:     $('a[href*=#]:not([href=#])').click(function() {     if (location.pathname.replace(/^\//,'') == this.pathname.replace(/^\//,'')          || location.hostname == this.hostname) {          var target = $(this.hash);         target = target.length ? target : $('[name=' + this.hash.slice(1) +']');            if (target.length) {              $('html,body').animate({                  scrolltop: target.offset().top             }, 1000);             return false;         }     } });      (   )    problem is, when i started using the smooth scrolling, the javascript tabs stopped working.    i think the problem is because the smoothscrolling affects the tabbed content links, but am not sure how to fix it.    i'd like to fix this is possible - but will resort to using a different script for either if necessary.    thanks in advance.  